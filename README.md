# DoodleBob
Script that takes an image, converts it into an image with a limited palette, and then generates trajectories to pass to a UR5 robot for it to draw.

Next step: Create an actual module such that we can use our code by referencing a module and a file name, as well as using launch files. Perhaps check old robotics lab where we used turtlesim. First just get the test code working but for the copied test code.

Uses https://ikpy.readthedocs.io/en/latest/
